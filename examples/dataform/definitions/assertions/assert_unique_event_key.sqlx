config {
  type: "assertion",
  schema: "hotel_search_silver",
  name: "assert_unique_event_key",
  tags: ["assertion", "dq", "silver"]
}

select
  partner_id,
  event_id,
  count(*) as duplicate_count
from ${ref("fact_search_event")}
group by partner_id, event_id
having count(*) > 1;
